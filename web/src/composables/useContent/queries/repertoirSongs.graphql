query getRepertoirSongs {
  scoreCollection(limit: 60) {
    items {
      title
      artist
      albumart {
        url
      }
      genreCollection {
        items {
          genre
        }
      }
    }
  }
}
